[pytest]
markers =
    smoke: fast tests suitable for every commit
    gpu: tests that require a GPU
    hive: tests involving cloud storage abstractions
    regression: tests pinning specific bug fixes
addopts = --strict-markers -v
testpaths = tests
