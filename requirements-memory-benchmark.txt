# Separate numerical proof tooling; production dependency files are unchanged.
# Tested versions are captured per run in environment.txt.
numpy>=1.24
pandas>=2.0
torch>=2.0
mpmath>=1.2
matplotlib>=3.6
pytest>=7
