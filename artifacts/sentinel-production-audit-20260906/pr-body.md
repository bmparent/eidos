Sentinel's advisory capacity check could admit simultaneous launches, and retrying a lost response could create a distinct job. Shared libSQL admission now reserves job capacity atomically, fences stale allocators, retains uncertain allocations until provider reconciliation, and maps browser retries and reloads to the same job. Missing metrics render as N/A, artifact errors keep the useful recovery explanation, and cleanup errors are surfaced.

Validation: 43 app tests; TypeScript and production build; 26 runner tests in Python 3.14.7/Torch 2.14.0+cpu; both bounded CPU profiles; 12 independent remote validation clients admitted one job at capacity one, with 11 capacity rejections and zero duplicate jobs across 12 retries. Rendered desktop/mobile tests passed launch, progress, download, reconnect, failures, provider retries, missing metrics, recorded-versus-run diagnostics, keyboard controls and reduced motion. Browser lifecycle fixtures used no production compute.

The authenticated live experiment is still blocked on the existing non-revealable production operator credential. No live job or live immutable artifact hashes are claimed. Authentication, engine behavior, thresholds, ordering, profiles, 20/60/20 splits and proof gates remain unchanged. Strict admission qualifies Sandbox experiment jobs; external-runner multi-instance admission and a global provider quota covering retrieval VMs are not qualified.

Evidence: apps/sentinel-lab/docs/audit-2026-09-06-followup.json, its Markdown companion, docs/shared-admission.md under the app, and docs/proof_runs/2026-09-06/. Full sanitized logs and screenshots are retained locally and mirrored to the configured Drive folder. The optional CI browser-upload patch was preserved separately because the existing GitHub credential lacks workflow scope; current CI and local browser verification passed.

Proof Logic + Meaning: occupied < capacity is checked in the same primary write transaction as reservation insertion, and retries preserve one job identity. This strengthens reproducibility and operator trust; it does not establish useful detection quality or held-out generalization. Zero proof gates advance.
