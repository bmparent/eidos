import { GuidedLab } from "@/components/guided-lab";

/** Preserve the existing charts, evidence questions, monitors and recovery tools. */
export default function AdvancedWorkspace() {
  return <GuidedLab />;
}
