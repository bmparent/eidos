import Link from "next/link";

export default function ConnectorGuide() {
  return <div className="guided-lab"><main className="gl-main"><Link href="/">← Sentinel Lab</Link><section className="gl-panel"><p className="gl-eyebrow">Scoped telemetry</p><h1>Connect only what you intend to observe.</h1><p>Create a source in Monitors, choose the exact metric name and unit, and save its ingest key in your collector environment. No collector is installed by this app.</p><ol><li>Use the packaged <code>connectors/sentinel/otel-collector.yaml</code> with an OpenTelemetry Contrib collector.</li><li>Set <code>EIDOS_INGEST_URL</code> to this app’s source endpoint and <code>EIDOS_INGEST_KEY</code> to the scoped key. Do not put the key in source control.</li><li>Send a matching gauge or sum metric. This version accepts OTLP/HTTP JSON, not protobuf, histograms or traces.</li><li>Check source health, buffered events and warmup. Process the first batch or explicitly enable automatic processing.</li><li>Replay the sample JSONL using <code>python connectors/sentinel/replay.py --file connectors/sentinel/sample.jsonl</code>. Replay keeps its own durable cursor and stable event IDs.</li></ol><h2>Operational bounds</h2><p>100 events/request; 1,000 pending events/source; 10,000 accepted events/day/source; four processing batches/day/source. The shared worker capacity remains one job. Requests over the buffer limit return 429 so the collector can retry from its persistent queue.</p><h2>What recovery means</h2><p>Arrival offsets and IDs are durable before acknowledgement. Replayed identical IDs do not increment counts. Reusing an ID for changed content fails. Late event times remain inspectable but do not rewind model state. Model state commits with the processed offset; a retry cannot apply the same batch twice.</p><p>Warmup covers the first 24 accepted events. Staleness is five minutes without arrivals. Gaps, resets, synthetic validation events and unreviewed anomalies appear separately. A checkpoint reset requires a reason and retains the previous state.</p><p>Automatic processing happens on incoming batches and is bounded; it does not install a scheduler. Open a saved job to reconcile completion after an interrupted browser session. Collector queues are finite: prolonged outages require operator attention to queue capacity and rejected exports.</p><h2>Revocation</h2><p>Use “Revoke ingestion keys” on the source to stop future ingestion immediately. Keys cannot read datasets, run results, stored vectors or another source’s configuration.</p><a href="https://opentelemetry.io/docs/collector/configuration/">Official Collector configuration ↗</a></section></main></div>;
}
